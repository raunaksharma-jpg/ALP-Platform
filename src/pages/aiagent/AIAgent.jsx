import React from "react";
import { Typography, Container } from "@mui/material";

const AIAgent = () => {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h4">AI Agent</Typography>
    </Container>
  );
};

export default AIAgent;



